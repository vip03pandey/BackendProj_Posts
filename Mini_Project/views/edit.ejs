<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white py-10 px-10">
        <div class="w-full flex justify-end">
            <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5">Logout</a>
        </div>
        <h3 class="text-white mb-3 mt-2 text-3xl"><span class="font-light">Hello</span>, <%= user.name %> ✌️</h3>
        <h5 class="mb-5 text-1xl">Edit your post</h5>
        <form action="/update/<%=post._id%>" method="post">
            <textarea name="content" class="w-full resize-none bg-transparent border-2 border-white-800 outline-none rounded-md p-2 mb-2" placeholder="What's on your mind"><%=post.content%></textarea>
            <input type="submit" class="px-3 py-2 bg-yellow-500 rounded-md" value="Update Post">
        </form>
    </div>
</body>
</html>
