<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white py-10 px-10">
        <div class="w-full flex justify-end">
            <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5">Logout</a>
        </div>
        <h3 class="text-white mb-3 mt-2 text-3xl"><span class="font-light">Hello</span>, <%= user.name %> ✌️</h3>
        <h5 class="mb-5 text-1xl">You can create a new post</h5>
        <form action="/post" method="post">
            <textarea name="content" class="w-full resize-none bg-transparent border-2 border-white-800 outline-none rounded-md p-2 mb-2" placeholder="What's on your mind"></textarea>
            <input type="submit" class="px-3 py-2 bg-blue-500 rounded-md" value="Create Post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-yellow-400 ml-3">Your Posts</h3>
            <div class="postcontainer mt-2">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 p-5 border-2 border-zinc-800 rounded-md">
                        <h4 class="text-blue-500 mb-2"><%= post.user.username %></h4> 
                        <p class="text-sm tracking-tighter"><%= post.content %></p>
                        <small class="mt-2 inline-block"><%= post.likes.length %> Likes</small> 
                        <div class="btns flex gap-3 mt-5">
                            <a class="text-blue-400" href="/like/<%= post._id %>"><%=post.likes.indexOf(user._id) ? "Like" :"UnLike" %></a>
                            <a class="text-red-500" href="/edit/<%=post._id%>">Edit</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>
